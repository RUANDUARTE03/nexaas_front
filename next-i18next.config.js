/* eslint-disable import/no-self-import */
const path = require('path');

module.exports = {
  i18n: {
    defaultLocale: 'pt',
    locales: ['pt'],
    localePath: path.resolve('./public/locales'),
  },
};
